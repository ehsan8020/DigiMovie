@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "ثبت نام ";
}
<br class="my-3" />
<h2 class="text-right">همگام سازی حساب کاربری @Model.LoginProvider </h2>
<hr />

<h5 class="text-info text-right">
    <i class="fas fa-check align-middle"></i>
    شما به وسیله <strong>@Model.LoginProvider</strong> با موفقیت احراز هویت گریده اید. حال جهت ورودبه سایت، ایمیل خود را ثبت نمایید.
</h5>

<div class="row">
    <div class="col-md-4">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="form-inline text-right">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.Email"></label>
                &nbsp; &nbsp;
                <input asp-for="Input.Email" class="form-control" />
                &nbsp; &nbsp;
                <span asp-validation-for="Input.Email" class="text-danger"></span>
                &nbsp; &nbsp;
                <button type="submit" class="btn btn-success">
                    <i class="fas fa-user-plus  align-middle"></i>
                    ثبت نام
                </button>
            </div>
           
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

