@page
@model PersonalDataModel
@{
    ViewData["Title"] = "اطلاعات شخصی";
    //ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="row">
    <div class="col-md">
        <p>حساب کاربری شما شامل اطلاعات شخصی می باشد که شما به ما داده اید. این صفحه به شما اجازه دانلود یا حذف آنها را می دهد.</p>
        <p>
            <strong>با حذف این اطلاعات ، حساب کاربری شما به طور دائمی حذف خواهد کرد به طوری که دیگر قابل بازیابی نخواهد بود.</strong>
        </p>
        <form asp-page="DownloadPersonalData" method="post" class="form-group">
            <button class="btn btn-outline-secondary" type="submit">
                <i class="fas fa-download"></i>
                دانلود
            </button>
            <a asp-page="DeletePersonalData" class="btn btn-danger">
                <i class="fas fa-trash"></i>
                حذف
            </a>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}